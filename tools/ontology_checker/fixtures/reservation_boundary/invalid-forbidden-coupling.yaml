case_id: reservation-boundary-invalid-forbidden-coupling
concept: ReservationBoundaryDataset
expected: {valid: false, error_codes: [RESERVATION_BOUNDARY_REQUEST_INVALID, RESERVATION_BOUNDARY_FORBIDDEN_COUPLING]}
dataset:
  establishment_request:
    request_id: REQUEST-SYNTH-FORBIDDEN
    branch: whole_resource_exclusivity
    action: reservation
    rule_ref: whole-resource-reservation-allocation-boundary@1
    resource_ref: RESOURCE-SYNTH-FORBIDDEN
    context_ref: CONTEXT-SYNTH-FORBIDDEN
    resource_snapshot_ref: SNAPSHOT-SYNTH-FORBIDDEN
    quantitative_contract_ref: null
    stored_result: indeterminate
    assignment_mutation: {synthetic: true}
  resource_snapshots:
    - snapshot_ref: SNAPSHOT-SYNTH-FORBIDDEN
      resource_ref: RESOURCE-SYNTH-FORBIDDEN
      context_ref: CONTEXT-SYNTH-FORBIDDEN
      evidence_state: current
      assignment_refs: [ASSIGNMENT-SYNTH-FORBIDDEN]
      constraint_evaluation_refs: []
      quantitative_input_snapshot_ref: null
