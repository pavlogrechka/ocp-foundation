case_id: order-authorization-boundary-invalid-record-order
concept: OrderAuthorizationBoundaryDataset
expected: {valid: false, error_codes: [ORDER_AUTHORIZATION_BOUNDARY_CONVENIENCE_SELECTOR_FORBIDDEN]}
dataset:
  boundary_request: {request_id: REQUEST-SYNTH-RECORD-ORDER, question: mandatory_order, rule_ref: mandatory-order-establishment-boundary@1, subject_operation_ref: OPERATION-SYNTH-RECORD-ORDER, authorization_snapshot_ref: SNAPSHOT-SYNTH-RECORD-ORDER, stored_result: indeterminate}
  authorization_snapshots: [{snapshot_ref: SNAPSHOT-SYNTH-RECORD-ORDER, subject_operation_ref: OPERATION-SYNTH-RECORD-ORDER, source_contract_ref: OCP-018@0.2.1, source_owner_ref: OWNER-SYNTH-RECORD-ORDER, input_snapshot_ref: INPUT-SYNTH-RECORD-ORDER, evaluation_context_ref: CONTEXT-SYNTH-RECORD-ORDER, evidence_state: current, source_result: accepted, order_candidate_ref: ORDER-CANDIDATE-SYNTH-RECORD-ORDER}]
  claims: {record_order: VALUE-SYNTH-RECORD-ORDER}
